name: Test LeetCode Solutions

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: List files
        run: |
          echo "Repository structure:"
          ls -la
          echo "Leetcode folder:"
          ls -la Leetcode/